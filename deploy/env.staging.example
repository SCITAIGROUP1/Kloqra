# Staging environment — copy values into Railway / Vercel dashboards.
# Generate secrets: bash scripts/deploy/generate-secrets.sh
# Full guide: docs/runbooks/railway.md

# --- Railway API service (chronomint-staging) ---
DATABASE_URL=${{Postgres.DATABASE_URL}}
REDIS_URL=<from Railway Redis plugin>
JWT_ACCESS_SECRET=<unique-32+-chars>
JWT_REFRESH_SECRET=<unique-32+-chars>
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
FRONTEND_ORIGIN=https://chronomint-client-staging.vercel.app,https://chronomint-admin-staging.vercel.app
PUBLIC_ADMIN_URL=https://chronomint-admin-staging.vercel.app
PORT=3001
NODE_ENV=production

# --- Vercel client (chronomint-client-staging, root apps/client) ---
NEXT_PUBLIC_API_BASE_URL=https://<staging-api>.up.railway.app
NEXT_PUBLIC_AUTH_SCOPE=client

# --- Vercel admin (chronomint-admin-staging, root apps/admin) ---
NEXT_PUBLIC_API_BASE_URL=https://<staging-api>.up.railway.app
NEXT_PUBLIC_AUTH_SCOPE=admin
NEXT_PUBLIC_ADMIN_URL=https://chronomint-admin-staging.vercel.app
