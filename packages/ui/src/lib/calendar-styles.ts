/** Muted marker for today — distinct from selected endpoints and in-range highlights. */
export const CALENDAR_TODAY_CLASS =
  "bg-muted font-semibold text-foreground ring-1 ring-inset ring-primary/35";
