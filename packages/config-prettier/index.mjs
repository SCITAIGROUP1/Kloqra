/** @type {import('prettier').Config} */
const config = {
  semi: true,
  singleQuote: false,
  trailingComma: "none",
  printWidth: 100,
  tabWidth: 2,
  arrowParens: "always"
};

export default config;
